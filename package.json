{
  "name": "green-buses-cross",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @green-buses/workbench",
    "build": "npm run build --workspace @green-buses/workbench",
    "preview": "npm run preview --workspace @green-buses/workbench",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
